#include <iostream>
#include <vector> 
#include "inc/interface_SpaceShip.h"

using namespace std; 

int main()
{
    //MillenniumFalcon mf();
    //Eagle5 e5(); 
    
    //MillenniumFalcon unholyMf(); 
    //Eagle5 unholyE5(); 

    std::vector<Iface_SpaceShip*> starFleet;

    //starFleet.push_back(&mf);
    //starFleet.push_back(&e5);
    //starFleet.push_back(&unholyMf);
    //starFleet.push_back(&unholyE5);

    //Loop through all, print their starting status, then take a hit, then print their final status. 
}